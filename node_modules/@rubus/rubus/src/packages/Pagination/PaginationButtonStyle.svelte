<script>
  /**
   * Specify the side of buttons
   * @type {"left" | "right" } [side = "left"]
   */
  export let side = "left";

  /**
   * Specify the variant of buttons
   * @type {"cta" | "primary" | "secondary" } [variant="cta"]
   */
  export let variants = "cta";
</script>

<nav class="spectrum-SplitButton spectrum-SplitButton--{side} spectrum-SplitButton--{variants}">
  <slot />
</nav>
