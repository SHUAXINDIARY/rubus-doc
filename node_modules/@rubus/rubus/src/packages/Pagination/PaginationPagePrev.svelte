<script>
  import Button from "../Button";

  /**
   * Set to `true` to disable the button
   * @type {boolean} [disabled=false]
   */
  export let disabled = false;
</script>

<Button variant="primary" isQuiet class="spectrum-Pagination-prevButton" {disabled} on:click on:keydown>Prev</Button>
