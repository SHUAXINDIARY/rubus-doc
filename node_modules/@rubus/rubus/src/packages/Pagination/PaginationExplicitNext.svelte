<script>
  import Button from "../Button";
  import { IconChevronRightMedium } from "@rubus/svelte-spectrum-icons-ui";

  /**
   * Set to `true` to disable the button
   * @type {boolean} [disabled=false]
   */
  export let disabled = false;
</script>

<Button exterior="action" isQuiet class="spectrum-Pagination-nextButton" {disabled} on:click on:keydown>
  <IconChevronRightMedium
    className="spectrum-Icon spectrum-UIIcon-ChevronRightMedium"
    focusable="false"
    aria-hidden="true" />
</Button>
