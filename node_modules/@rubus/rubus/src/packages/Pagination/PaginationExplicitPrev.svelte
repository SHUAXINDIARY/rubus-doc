<script>
  import Button from "../Button";
  import { IconChevronLeftMedium } from "@rubus/svelte-spectrum-icons-ui";

  /**
   * Set to `true` to disable the button
   * @type {boolean} [disabled=false]
   */
  export let disabled = false;
</script>

<Button exterior="action" isQuiet class="spectrum-Pagination-prevButton" {disabled} on:click on:keydown>
  <IconChevronLeftMedium
    className="spectrum-Icon spectrum-UIIcon-ChevronLeftMedium"
    focusable="false"
    aria-hidden="true" />
</Button>
