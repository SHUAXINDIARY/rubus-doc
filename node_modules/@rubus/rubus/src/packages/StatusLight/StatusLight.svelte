<script>
  /**
   * Specify the variants  for the StatusLight
   * @type { "active"
            | "celery"
            | "chartreuse"
            | "fuchsia"
            | "indigo"
            | "info"
            | "magenta"
            | "negative"
            | "neutral"
            | "notice"
            | "positive"
            | "purple"
            | "seafoam" | "yellow"}  [orientation = "horizontal"]
   */
  export let variants = "celery";

  /**
   * Set to `true` to disable the StatusLight
   * @type {boolean}[disabled=false]
   */
  export let disabled = false;
</script>

<div class="spectrum-StatusLight spectrum-StatusLight--{variants}" class:is-disabled={disabled}>
  <slot />
</div>
