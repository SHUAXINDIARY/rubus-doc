<script>
  /**
   * Specify the open status of Modal
   * @type {number} [zIndex = 100]
   */
  export let isOpen = false;

  /**
   * Specify the css `z-index` of Modal
   * @type {number} [zIndex = 100]
   */
  export let zIndex = 100;
</script>

<div class="spectrum-Modal-wrapper {$$restProps.class}" style="z-index:{+zIndex}">
  <div class="spectrum-Modal {$$restProps.innerClass}" class:is-open={isOpen} style={$$restProps.innerStyle}>
    <slot />
  </div>
</div>
