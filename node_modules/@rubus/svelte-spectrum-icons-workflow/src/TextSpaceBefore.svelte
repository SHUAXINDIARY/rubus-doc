<script>
  import { afterUpdate } from "svelte";
  import { viewBoxes } from "./util.js";
   export let scale = "M";  
  export let width = "100%";
  export let height = "100%";
  export let className = "";
  
  export let ariaLabel = "TextSpaceBefore";
  let autoScale = 18;
  afterUpdate(() => {
    let rootClassName = document && document.documentElement.className;
    if (rootClassName && rootClassName.indexOf("spectrum--large") != -1) {
      autoScale = 22;
    }
  });
</script>

<svg
  viewBox={viewBoxes[scale]}
  aria-label={ariaLabel}
  {...$$restProps}
  width={width !== '100%' ? width : autoScale}
  height={height !== '100%' ? height : autoScale}
  fill="currentColor"
  class="spectrum-Icon spectrum-Icon-workflow {className}">
  <rect height={4} rx={1} ry={1} width={22} x={12} y={24} />
  <rect height={4} rx={1} ry={1} width={22} x={12} y={18} />
  <rect height={4} rx={1} ry={1} width={22} x={12} y={30} />
  <path
    d="M2 2.672a.5.5 0 0 1 .866-.341L8 8l-5.134 5.669A.5.5 0 0 1 2 13.328zM33 2H13a1 1 0 0 0-1 1v10a1 1 0 0 0 1 1h20a1 1
    0 0 0 1-1V3a1 1 0 0 0-1-1zm-1 10H14V4h18z" />
</svg>
