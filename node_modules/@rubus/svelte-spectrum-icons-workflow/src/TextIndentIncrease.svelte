<script>
  import { afterUpdate } from "svelte";
  import { viewBoxes } from "./util.js";
   export let scale = "M";  
  export let width = "100%";
  export let height = "100%";
  export let className = "";
  
  export let ariaLabel = "TextIndentIncrease";
  let autoScale = 18;
  afterUpdate(() => {
    let rootClassName = document && document.documentElement.className;
    if (rootClassName && rootClassName.indexOf("spectrum--large") != -1) {
      autoScale = 22;
    }
  });
</script>

<svg
  viewBox={viewBoxes[scale]}
  aria-label={ariaLabel}
  {...$$restProps}
  width={width !== '100%' ? width : autoScale}
  height={height !== '100%' ? height : autoScale}
  fill="currentColor"
  class="spectrum-Icon spectrum-Icon-workflow {className}">
  <rect height={4} rx={1} ry={1} width={24} x={8} y={28} />
  <rect height={4} rx={1} ry={1} width={12} x={20} y={20} />
  <rect height={4} rx={1} ry={1} width={12} x={20} y={12} />
  <rect height={4} rx={1} ry={1} width={24} x={8} y={4} />
  <path
    d="M8 14v-3.328a.5.5 0 0 1 .866-.341L16 18l-7.134 7.669A.5.5 0 0 1 8 25.328V22H1a1 1 0 0 1-1-1v-6a1 1 0 0 1 1-1z" />
</svg>
