<script>
  import { afterUpdate } from "svelte";
  import { viewBoxes } from "./util.js";
   export let scale = "M";  
  export let width = "100%";
  export let height = "100%";
  export let className = "";
  
  export let ariaLabel = "JourneyReports";
  let autoScale = 18;
  afterUpdate(() => {
    let rootClassName = document && document.documentElement.className;
    if (rootClassName && rootClassName.indexOf("spectrum--large") != -1) {
      autoScale = 22;
    }
  });
</script>

<svg
  viewBox={viewBoxes[scale]}
  aria-label={ariaLabel}
  {...$$restProps}
  width={width !== '100%' ? width : autoScale}
  height={height !== '100%' ? height : autoScale}
  fill="currentColor"
  class="spectrum-Icon spectrum-Icon-workflow {className}">
  <rect height={18} rx={0.5} width={2} x={34} y={18} />
  <rect height={12} rx={0.5} width={2} x={30} y={24} />
  <rect height={8} rx={0.5} width={2} x={26} y={28} />
  <rect height={6} rx={0.5} width={2} x={22} y={30} />
  <path
    d="M20 28a2 2 0 0 1-2-2V10a2 2 0 0 1 2-2h4.1a5 5 0 1 0 0-2H20a4 4 0 0 0-4 4v6h-4.1a5 5 0 1 0 0 2H16v8a4 4 0 0 0 4
    4zm9-24a3 3 0 1 1-3 3 3 3 0 0 1 3-3zM7 20a3 3 0 1 1 3-3 3 3 0 0 1-3 3z" />
</svg>
