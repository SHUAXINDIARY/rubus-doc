<script>
  import { afterUpdate } from "svelte";
  import { viewBoxes } from "./util.js";
   export let scale = "M";  
  export let width = "100%";
  export let height = "100%";
  export let className = "";
  
  export let ariaLabel = "MBox";
  let autoScale = 18;
  afterUpdate(() => {
    let rootClassName = document && document.documentElement.className;
    if (rootClassName && rootClassName.indexOf("spectrum--large") != -1) {
      autoScale = 22;
    }
  });
</script>

<svg
  viewBox={viewBoxes[scale]}
  aria-label={ariaLabel}
  {...$$restProps}
  width={width !== '100%' ? width : autoScale}
  height={height !== '100%' ? height : autoScale}
  fill="currentColor"
  class="spectrum-Icon spectrum-Icon-workflow {className}">
  <path d="M33 4H3a1 1 0 0 0-1 1v26a1 1 0 0 0 1 1h30a1 1 0 0 0 1-1V5a1 1 0 0 0-1-1zm-1 26H4V10h28z" />
  <path
    d="M6 12h2v2H6zM6 22h2v2H6zM10 12h4v2h-4zM16 12h4v2h-4zM22 12h4v2h-4zM10 26h4v2h-4zM16 26h4v2h-4zM22 26h4v2h-4zM28
    12h2v2h-2zM28 16h2v2h-2zM6 16h2v4H6zM28 20h2v4h-2zM6 26h2v2H6zM28 26h2v2h-2z" />
</svg>
