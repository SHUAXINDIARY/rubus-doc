<script>
  import { afterUpdate } from "svelte";
  import { viewBoxes } from "./util.js";
   export let scale = "M";  
  export let width = "100%";
  export let height = "100%";
  export let className = "";
  
  export let ariaLabel = "TextSpaceAfter";
  let autoScale = 18;
  afterUpdate(() => {
    let rootClassName = document && document.documentElement.className;
    if (rootClassName && rootClassName.indexOf("spectrum--large") != -1) {
      autoScale = 22;
    }
  });
</script>

<svg
  viewBox={viewBoxes[scale]}
  aria-label={ariaLabel}
  {...$$restProps}
  width={width !== '100%' ? width : autoScale}
  height={height !== '100%' ? height : autoScale}
  fill="currentColor"
  class="spectrum-Icon spectrum-Icon-workflow {className}">
  <rect height={4} rx={1} ry={1} width={20} x={14} y={8} />
  <rect height={4} rx={1} ry={1} width={20} x={14} y={14} />
  <rect height={4} rx={1} ry={1} width={20} x={14} y={2} />
  <path
    d="M4 33.328a.5.5 0 0 0 .866.341L10 28l-5.134-5.669a.5.5 0 0 0-.866.341zM34 33V23a1 1 0 0 0-1-1H15a1 1 0 0 0-1
    1v10a1 1 0 0 0 1 1h18a1 1 0 0 0 1-1zm-2-1H16v-8h16z" />
</svg>
